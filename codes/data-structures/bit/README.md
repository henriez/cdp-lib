# BIT (Fenwick Tree)
Data structure that can handle point/range update and point/range queries for invertible operations.
Obs: If the operation is not easily invertible (e.g. min,max,gcd), it can only answer prefix queries