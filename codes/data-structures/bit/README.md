# BIT (Fenwick Tree)
Data structure that can handle point update and range queries for invertible operations.
Obs: If the operation is not invertible (e.g. min,max,gcd), it can only answer prefix queries